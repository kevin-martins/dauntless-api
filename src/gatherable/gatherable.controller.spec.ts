import { Test, TestingModule } from "@nestjs/testing";
import { GatherableController } from "./gatherable.controller";

// describe('GatherableController', () => {
//   let controller: GatherableController;

//   beforeEach(async () => {
//     const module: TestingModule = await Test.createTestingModule({
//       controllers: [GatherableController],
//     }).compile();

//     controller = module.get<GatherableController>(GatherableController);
//   });

//   it('should be defined', () => {
//     expect(controller).toBeDefined();
//   });
// });
